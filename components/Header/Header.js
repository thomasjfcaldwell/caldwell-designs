import React from 'react';
import { Container, Title, SubTitle } from './HeaderStyles';

const Header = () => {
	return (
		<Container>
			<Title>Thomas Caldwell</Title>
			<SubTitle>Frontend Web Developer</SubTitle>
		</Container>
	);
};

export default Header;
